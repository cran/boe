% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe_mpc_decisions.R
\name{boe_mpc_decisions}
\alias{boe_mpc_decisions}
\title{Bank Rate decision history}
\source{
Derived from BoE series \code{IUDBEDR} (daily Bank Rate). See
\url{https://www.bankofengland.co.uk/monetary-policy}.
}
\usage{
boe_mpc_decisions(from = "1997-06-06", to = Sys.Date(), cache = TRUE)
}
\arguments{
\item{from}{Date or character. Start date. Defaults to
\code{"1997-06-06"} (the first MPC meeting).}

\item{to}{Date or character. End date. Defaults to today.}

\item{cache}{Logical. Use cached Bank Rate data if available
(default \code{TRUE}).}
}
\value{
A \code{boe_tbl} data frame with columns:
\describe{
\item{date}{Date. Effective date of the rate change.}
\item{new_rate_pct}{Numeric. Bank Rate after the decision (percent).}
\item{prev_rate_pct}{Numeric. Bank Rate before the decision (percent).}
\item{change_bps}{Integer. Change in basis points (positive = hike,
negative = cut).}
\item{direction}{Character. \code{"hike"} or \code{"cut"}.}
}
}
\description{
Returns the history of Monetary Policy Committee decisions to change
Bank Rate, derived from the daily Bank Rate series. Each row is one
rate-change event, showing the effective date, the new rate, the
previous rate, and the change in basis points. Holds (meetings where
the rate was unchanged) are not included; for the full meeting-level
record including holds, see \code{\link[=boe_mpc_votes]{boe_mpc_votes()}}.
}
\examples{
\donttest{
op <- options(boe.cache_dir = tempdir())
# All MPC decisions since the global financial crisis
boe_mpc_decisions(from = "2007-01-01")

# Just decisions in 2024 to date
boe_mpc_decisions(from = "2024-01-01")
options(op)
}

}
\seealso{
\code{\link[=boe_bank_rate]{boe_bank_rate()}}, \code{\link[=boe_mpc_votes]{boe_mpc_votes()}}

Other monetary policy: 
\code{\link{boe_mpc_votes}()},
\code{\link{boe_mpr_forecasts}()}
}
\concept{monetary policy}
