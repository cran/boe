% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe_mpc_votes.R
\name{boe_mpc_votes}
\alias{boe_mpc_votes}
\title{Monetary Policy Committee voting history}
\source{
\url{https://www.bankofengland.co.uk/monetary-policy}
}
\usage{
boe_mpc_votes(cache = TRUE)
}
\arguments{
\item{cache}{Logical. Use cached download if less than 24 hours old
(default \code{TRUE}). The voting workbook is small (~110 KB).}
}
\value{
A \code{boe_tbl} data frame with columns:
\describe{
\item{date}{Date. Meeting date.}
\item{member}{Character. MPC member name.}
\item{member_vote_pct}{Numeric. The Bank Rate the member voted
for (percent).}
\item{decision_pct}{Numeric. The committee's decision (percent).}
\item{dissent}{Logical. \code{TRUE} if the member's vote differed from
the committee decision.}
}
}
\description{
Downloads the Bank of England's published MPC voting record and
returns it in long format. Each row is one (meeting, member) pair
showing the rate the member voted for and whether that constituted
a dissent from the committee's decision.
}
\details{
Coverage runs from the first MPC meeting in June 1997 through the
most recent published minutes. Both current and past committee
members are included.

Requires the \pkg{readxl} package.
}
\examples{
\donttest{
if (requireNamespace("readxl", quietly = TRUE)) {
  op <- options(boe.cache_dir = tempdir())
  votes <- boe_mpc_votes()

  # Recent dissents
  recent <- subset(votes, dissent & date >= as.Date("2024-01-01"))
  head(recent)

  options(op)
}
}

}
\seealso{
\code{\link[=boe_mpc_decisions]{boe_mpc_decisions()}}

Other monetary policy: 
\code{\link{boe_mpc_decisions}()},
\code{\link{boe_mpr_forecasts}()}
}
\concept{monetary policy}
