% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe_search.R
\name{boe_search}
\alias{boe_search}
\title{Search the BoE series catalogue}
\usage{
boe_search(query = NULL, category = NULL, frequency = NULL)
}
\arguments{
\item{query}{Character. Keyword(s) to match against the \code{title} and
\code{code} columns. Case-insensitive substring match. If \code{NULL},
returns all rows (subject to other filters).}

\item{category}{Character. Optional filter on the \code{category} column,
one or more of \code{"interest_rates"}, \code{"exchange_rates"},
\code{"mortgage_market"}, \code{"consumer_credit"}, \code{"monetary_aggregates"}.}

\item{frequency}{Character. Optional filter on the \code{frequency}
column, one or more of \code{"daily"}, \code{"monthly"}, \code{"annual"}.}
}
\value{
A data frame with the same columns as \link{boe_series},
restricted to matching rows.
}
\description{
Filters the \link{boe_series} catalogue by keyword and optional category
and frequency. Useful for finding a series code without leaving R.
Equivalent to a \code{grepl} against the title and code columns.
}
\examples{
# Find mortgage-related series
boe_search("mortgage")

# All daily interest-rate series
boe_search(category = "interest_rates", frequency = "daily")

# Locate the Bank Rate code
boe_search("bank rate")

}
\seealso{
\code{\link[=boe_browse]{boe_browse()}}, \link{boe_series}

Other discovery: 
\code{\link{boe_browse}()}
}
\concept{discovery}
