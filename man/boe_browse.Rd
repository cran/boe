% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe_search.R
\name{boe_browse}
\alias{boe_browse}
\title{Browse the BoE series catalogue}
\usage{
boe_browse(category = NULL, frequency = NULL)
}
\arguments{
\item{category}{Character. Optional filter on the \code{category} column,
one or more of \code{"interest_rates"}, \code{"exchange_rates"},
\code{"mortgage_market"}, \code{"consumer_credit"}, \code{"monetary_aggregates"}.}

\item{frequency}{Character. Optional filter on the \code{frequency}
column, one or more of \code{"daily"}, \code{"monthly"}, \code{"annual"}.}
}
\value{
A data frame with the same columns as \link{boe_series}.
}
\description{
Returns the catalogue with optional category or frequency filters.
Equivalent to \code{boe_search(query = NULL, category, frequency)} but
framed as a browse / inspect action rather than a keyword search.
}
\examples{
# The whole catalogue
nrow(boe_browse())

# All exchange rate series
boe_browse(category = "exchange_rates")

# All monthly series
boe_browse(frequency = "monthly")

}
\seealso{
\code{\link[=boe_search]{boe_search()}}, \link{boe_series}

Other discovery: 
\code{\link{boe_search}()}
}
\concept{discovery}
