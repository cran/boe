% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boe_get.R
\name{boe_get}
\alias{boe_get}
\title{Fetch any series from the Bank of England Statistical Database}
\source{
\url{https://www.bankofengland.co.uk/boeapps/database/}
}
\usage{
boe_get(series_codes, from = "1960-01-01", to = Sys.Date(), cache = TRUE)
}
\arguments{
\item{series_codes}{Character vector of one or more BoE series codes.}

\item{from}{Date or character (YYYY-MM-DD). Start date. Defaults to
\code{"1960-01-01"}.}

\item{to}{Date or character (YYYY-MM-DD). End date. Defaults to today.}

\item{cache}{Logical. Use cached data if available (default \code{TRUE}).}
}
\value{
A data frame with columns:
\describe{
\item{date}{Date. Observation date.}
\item{code}{Character. BoE series code.}
\item{value}{Numeric. Observation value.}
}
}
\description{
The core data retrieval function. Fetches one or more series by their
BoE series codes and returns a tidy data frame. Use this when the
convenience functions (e.g. \code{\link[=boe_bank_rate]{boe_bank_rate()}}, \code{\link[=boe_exchange_rate]{boe_exchange_rate()}})
do not cover the series you need.
}
\details{
Series codes can be found via the Bank of England Interactive
Statistical Database at
\url{https://www.bankofengland.co.uk/boeapps/database/}.
}
\examples{
\donttest{
op <- options(boe.cache_dir = tempdir())
# Bank Rate since 2000
boe_get("IUDBEDR", from = "2000-01-01")

# Multiple series
boe_get(c("IUDBEDR", "IUDSOIA"), from = "2020-01-01")
options(op)
}

}
\seealso{
Other data access: 
\code{\link{clear_cache}()}
}
\concept{data access}
